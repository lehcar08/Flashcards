# Flashcards
A simple Python flashcard program that quizzes the user on terms and definitions.

## How to run
python main.py

## Important Warning!
This program requires valid inputs at each prompt. Otherwise, the program may behave incorrectly or stop running.
